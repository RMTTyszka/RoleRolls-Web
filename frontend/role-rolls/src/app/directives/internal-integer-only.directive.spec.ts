import { InternalIntegerOnlyDirective } from './internal-integer-only.directive';

describe('InternalIntegerOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new InternalIntegerOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
