import { FieldTitleDirective } from './field-title.directive';

describe('FieldTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldTitleDirective();
    expect(directive).toBeTruthy();
  });
});
